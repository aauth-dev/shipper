{
  "name": "shipper",
  "version": "0.1.0",
  "private": true,
  "description": "Consumes the aauth-events Cloudflare Queue, signs batches as shipper.aauth.dev (jwks_uri scheme), and POSTs NDJSON to Freezer ingest. Also serves the .well-known/ metadata Freezer fetches to verify the signature.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail shipper",
    "generate-key": "node scripts/generate-key.mjs"
  },
  "dependencies": {
    "@hellocoop/httpsig": "^1.5.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
